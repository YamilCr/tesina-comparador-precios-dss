"""Decisiones de persistencia del dominio de canastas temporales.

En esta versión no existe repositorio de canastas. La canasta se construye a
partir del request de un usuario anónimo y permanece en memoria. Más adelante
podría agregarse persistencia si se implementan usuarios o canastas guardadas.
"""
